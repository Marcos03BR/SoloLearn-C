// Create an app to calculate the percentage.

#include <stdio.h>

int main() {
    int number = 250;
    int perc = 24;

    int result = (perc*number)/100;

    printf("%d", result);

    return 0;
}