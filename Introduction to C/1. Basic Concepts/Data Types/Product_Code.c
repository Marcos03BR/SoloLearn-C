// Add the required data types to the variables to complete the code.

#include <stdio.h>

int main() {
    char letter = 'B';
    int number = 127;

    printf("%c%d", letter, number);

    return 0;
}